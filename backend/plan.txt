//app.set('static', path.join(__dirname, 'public'))

/*
    DATA (data modeling)

    * one to one
    * one ot many
    * many to many
  
    SQL

    user 
        username, contact

    [
        { userId username, contact }
        { userId username, contact }
        { userId username, contact }
        { userId username, contact }
    ]

    food
        name 
    [
        { foodId foodName foodImg }
        { foodId foodName foodImg }
        { foodId foodName foodImg }
        { foodId foodName foodImg }
        { foodId foodName foodImg }
    ]

    order
    { orderId foodId userId count }
    { orderId foodId userId count }
    { orderId foodId userId count }

    User {
        UserId: Int
        username: String
        contact: String
    }

    Order {
        orderId: Int
        user: User
        food: Food
        count: Int
    }

    Food {
        foodId: Int
        foodName: String
        foodImg: String
    }

    Response {
        status: Int
        message: String
        data: Type
    }

    GET
    /users -> [User]
    /orders -> [Order]
    /foods -> [Food]

    POST
    /users [username, contact]     -> Response
    /order [userId, foodId, count] -> Response
*/
